# 多人游戏 WebSocket 地址（构建期注入，前端用 import.meta.env.VITE_GAME_WS 读取）
#
# 留空 / 不写此文件 → 默认连线上后端 wss://game.deepsucker.top（开箱即玩，无需本地后端）
#
# 本地联调时需要自己跑 deepsucker-game-server（wrangler dev 监听 8787），
# 复制本文件为 docs/.env 并改成下面这行，dev server 才会指向本地后端：
VITE_GAME_WS=ws://localhost:8787

# 注意：浏览器里临时换后端还有更高优先的 localStorage 覆盖，console 执行：
#   localStorage.setItem('ds-game-ws', 'wss://game.deepsucker.top')
# 设错/想还原就 localStorage.removeItem('ds-game-ws')

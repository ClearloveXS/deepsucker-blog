name = "deepsucker-game-server"
main = "src/index.ts"
compatibility_date = "2026-09-01"

[[durable_objects.bindings]]
name = "GAME_ROOM"
class_name = "GameRoom"

# 免费版只能用 SQLite-backed Durable Objects（new_sqlite_classes，不是 new_classes）
[[migrations]]
tag = "v1"
new_sqlite_classes = ["GameRoom"]
